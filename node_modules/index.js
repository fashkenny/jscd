const Express = require("express")

require("../mongoDB/Config/dataBase")

const port = 2500
const app = Express()

app.use(Express.json())

// TESTING SERVER
app.get('/', (req, res) => {
    res.status(200).json({
        message: "Server ready consumption"
    })
})

app.listen(port, () =>{
    console.log("listening on port, server now live 🚀🚀🚀", port)
})

